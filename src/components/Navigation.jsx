import React from 'react';
import { NavLink } from "react-router-dom";

const Menu = () => {
  
  return (

    <nav className="">
      <NavLink to="/admin">Listado</NavLink>
      <NavLink to="/admin">Listado</NavLink>
    </nav>

  )

}

export default Menu;
